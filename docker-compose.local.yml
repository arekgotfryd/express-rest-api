services:
  app:
    build:
      context: .
      target: builder
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "9229:9229"
    command: npx tsx watch --clear-screen=false --inspect=0.0.0.0:9229 src/index.ts
